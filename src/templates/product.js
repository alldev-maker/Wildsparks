import React from "react"
import Layout from "../components/layout"

const Product = ({ data }) => {
  return (
    <Layout>
      <section className="section product-detail">Product</section>
    </Layout>
  )
}

export default Product
