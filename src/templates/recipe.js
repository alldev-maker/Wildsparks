import React from "react"
import Layout from "../components/layout"

const Recipe = ({ data }) => {
  return (
    <Layout>
      <section className="section recipes-section">Recipe</section>
    </Layout>
  )
}

export default Recipe
