{
  "Main": {
    "cuisine": {
      "type": "Select",
      "config": {
        "options": ["Continental", "Caribbean", "African"],
        "default_value": "Continental",
        "label": "cuisine_type",
        "placeholder": "Cuisine Type"
      }
    },
    "uid": {
      "type": "UID",
      "config": {
        "label": "id"
      }
    },
    "title": {
      "type": "Text",
      "config": {
        "label": "Title",
        "placeholder": "Title"
      }
    },
    "header_image": {
      "type": "Image",
      "config": {
        "constraint": {
          "width": 700,
          "height": 500
        },
        "thumbnails": [],
        "label": "header_image"
      }
    },
    "description": {
      "type": "Text",
      "config": {
        "label": "Description",
        "placeholder": "Description"
      }
    },
    "total_time": {
      "type": "Number",
      "config": {
        "label": "Total Time",
        "placeholder": "Total Time"
      }
    },
    "prep_time": {
      "type": "Number",
      "config": {
        "label": "Prep Time",
        "placeholder": "Prep Time"
      }
    },
    "cook_time": {
      "type": "Number",
      "config": {
        "label": "Cook Time",
        "placeholder": "Cook Time"
      }
    },
    "servings": {
      "type": "Number",
      "config": {
        "label": "Servings",
        "placeholder": "Servings"
      }
    },
    "rating": {
      "type": "Number",
      "config": {
        "label": "Rating",
        "placeholder": "Rating"
      }
    },
    "ingredients": {
      "type": "StructuredText",
      "config": {
        "multi": "paragraph, preformatted, heading1, heading2, heading3, heading4, heading5, heading6, strong, em, hyperlink, image, embed, list-item, o-list-item, rtl",
        "label": "Ingredients",
        "placeholder": "Ingredients"
      }
    },
    "instructions": {
      "type": "StructuredText",
      "config": {
        "multi": "paragraph, preformatted, heading1, heading2, heading3, heading4, heading5, heading6, strong, em, hyperlink, image, embed, list-item, o-list-item, rtl",
        "label": "Instructions",
        "placeholder": "Instructions"
      }
    },
    "tips": {
      "type": "StructuredText",
      "config": {
        "multi": "paragraph, preformatted, heading1, heading2, heading3, heading4, heading5, heading6, strong, em, hyperlink, image, embed, list-item, o-list-item, rtl",
        "label": "Tips",
        "placeholder": "Tips"
      }
    }
  }
}
